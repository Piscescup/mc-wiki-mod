# Gradle
org.gradle.jvmargs=-Xmx2G
# The preprocess plugin links versioned subprojects and resolves sibling classpaths
# during task execution, which is not safe under Gradle 9 parallel project builds.
org.gradle.parallel=false
org.gradle.configuration-cache=false

# Shared Fabric properties
loader_version=0.19.3
mod_loader=fabric

# Mod properties
mod_id=mc-wiki
mod_name=Minecraft Wiki Mod
mod_version=1.0.0
maven_group=io.github.piscescup.mcwiki
archives_base_name=mc-wiki
